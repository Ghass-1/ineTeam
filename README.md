# ineteam

A new Flutter project for matchmaking.

## Getting Started

Make sure to have installed flutter SDK, if not:
- [Download flutter SDK](https://docs.flutter.dev/install/manual)

Clone the repository:
```bash
git clone https://github.com/Ghass-1/ineTeam
cd /ineTeam

Run the project, specify a browser or emulator to your choosing (chrome recommended):
```bash
flutter run -d chrome
